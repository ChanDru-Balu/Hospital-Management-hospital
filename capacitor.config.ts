import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'chan.hospital.hospital',
  appName: 'HosMan Hospital',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
